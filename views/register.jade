extends layout

block content
  h2.title Register

  if error
    - for (var i=0; i<error.length; i++)
      .error #{error[i]}

  .form-style-8
    form(method="post")
      span First Name:
      input(type="text", name="firstName", value=firstName, required=true)
      br

      span Last Name:
      input(type="text", name="lastName", value=lastName, required=true)
      br

      span Email:
      input(type="email", name="email", value=email, required=true)
      br
      
      span Username:
      input(type="text", name="userName", value=userName, required=true)
      br

      span Password:
      input(type="password", name="password", required=true)
      br

      span Confirm Password:
      input(type="password", name="confirmPassword", required=true)
      br

      input(type="submit")
